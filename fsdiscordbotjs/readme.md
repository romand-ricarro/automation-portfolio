* Create `.env` file which store
    * DISCORD_BOT_TOKEN
    * NAME_CHANNEL_ID (replace NAME with your channel)
    * SPREADSHEET_ID
* Create `creds.json` file to store Google key
* run `npm install`
